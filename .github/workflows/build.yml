name: Build YWD-RF

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

jobs:
  platformio:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install PlatformIO
        run: pip install platformio

      - name: Build firmware
        run: pio run -e ywd_rf -e ywd_rf_diag -e ywd_rf_probe

      - name: Upload firmware artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ywd-rf-firmware-${{ github.sha }}
          path: |
            .pio/build/ywd_rf/firmware.bin
            .pio/build/ywd_rf/bootloader.bin
            .pio/build/ywd_rf/partitions.bin
            .pio/build/ywd_rf_diag/firmware.bin
            .pio/build/ywd_rf_diag/bootloader.bin
            .pio/build/ywd_rf_diag/partitions.bin
            .pio/build/ywd_rf_probe/firmware.bin
            .pio/build/ywd_rf_probe/bootloader.bin
            .pio/build/ywd_rf_probe/partitions.bin
          if-no-files-found: warn
